// Upgraded to use version catalog for dependency management
plugins {
    alias(libs.plugins.android.application) apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
